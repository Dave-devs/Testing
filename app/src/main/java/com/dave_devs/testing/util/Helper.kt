package com.dave_devs.testing.util

class Helper {

    fun isNumberPositive(input: Int): Boolean {
        return input > 0
    }

    //val users = listOf("Peter", "John", "Smith")

    fun signUp(
        userName: String,
        password: String,
        repeatPassword: String
    ): Boolean{
        return true
    }
}

